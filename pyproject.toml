[project]
name = "pylambdac"
version = "0.1.0"
description = ""
authors = [{name = "Paul Crowley", email = "paulcrowley@google.com"}]
requires-python = ">=3.7"
dependencies = [
    "lark-parser>=0.8.2,<0.9",
    "mistletoe>=0.7.2,<0.8",
    "svgwrite>=1.4,<2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pylambdac"]
