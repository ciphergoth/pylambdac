start: directive ";"
?directive: let | test
let: "let" VAR "=" apply
test: "test" apply "==" apply
?apply: subexpr | mlambda | apply subexpr
?subexpr: VAR | "(" apply ")"
mlambda: ("λ" | "\\") VAR* "." apply
VAR: ("_"|LETTER|DIGIT)+

%import common.LETTER
%import common.DIGIT
%import common.WS
%ignore WS
